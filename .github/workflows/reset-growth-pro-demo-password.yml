name: Reset Growth Pro demo password

on:
  push:
    branches:
      - main
    paths:
      - scripts/reset-growth-pro-demo-password.mjs
      - .github/workflows/reset-growth-pro-demo-password.yml

permissions:
  contents: read
  statuses: write

concurrency:
  group: aqwelia-growth-pro-demo-password-reset
  cancel-in-progress: false

jobs:
  reset:
    name: Reset and verify Growth Pro demo account
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    env:
      DATABASE_URL: ${{ secrets.PRODUCTION_DIRECT_URL }}
    steps:
      - name: Publish pending status
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl --fail-with-body --silent --show-error \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -d "{\"state\":\"pending\",\"context\":\"AQWELIA Growth Pro login reset\",\"description\":\"Réinitialisation du compte démo Growth Pro\",\"target_url\":\"${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}\"}"
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.4'
      - run: bun install --frozen-lockfile
      - name: Validate direct production connection
        run: |
          node - <<'NODE'
          const url = process.env.DATABASE_URL || ''
          if (!/^postgres(ql)?:\/\//.test(url)) throw new Error('PRODUCTION_DIRECT_URL is required')
          if (new URL(url).hostname.includes('-pooler')) throw new Error('Direct Neon connection required')
          NODE
      - name: Generate PostgreSQL client
        run: bun run db:pg:generate
      - name: Reset Growth Pro demo password
        id: reset_account
        run: node scripts/reset-growth-pro-demo-password.mjs
      - name: Publish final status
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          RESET_OUTCOME: ${{ steps.reset_account.outcome }}
        run: |
          if [ "${RESET_OUTCOME}" = "success" ]; then
            STATE=success
            DESCRIPTION="Compte Growth Pro réinitialisé et vérifié"
          else
            STATE=failure
            DESCRIPTION="Échec de réinitialisation Growth Pro"
          fi
          curl --fail-with-body --silent --show-error \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -d "{\"state\":\"${STATE}\",\"context\":\"AQWELIA Growth Pro login reset\",\"description\":\"${DESCRIPTION}\",\"target_url\":\"${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}\"}"
