name: Apply P1-B intervention reassignment

on:
  push:
    branches:
      - feat/p1-b-team-dispatch
    paths:
      - scripts/apply-p1-b-intervention-reassignment.py
      - .github/workflows/apply-p1-b-intervention-reassignment.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/p1-b-team-dispatch
          fetch-depth: 1
      - run: python scripts/apply-p1-b-intervention-reassignment.py
      - run: |
          rm scripts/apply-p1-b-intervention-reassignment.py
          rm .github/workflows/apply-p1-b-intervention-reassignment.yml
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'feat(p1-b): allow controlled intervention reassignment'
          git push origin HEAD:feat/p1-b-team-dispatch
