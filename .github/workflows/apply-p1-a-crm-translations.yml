name: Apply P1-A CRM translations

on:
  push:
    branches:
      - feat/p1-a-pro-crm-foundation
    paths:
      - scripts/apply-p1-a-crm-translations.py
      - .github/workflows/apply-p1-a-crm-translations.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/p1-a-pro-crm-foundation
          fetch-depth: 1
      - run: python scripts/apply-p1-a-crm-translations.py
      - run: |
          rm scripts/apply-p1-a-crm-translations.py
          rm .github/workflows/apply-p1-a-crm-translations.yml
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'feat(p1-a): translate Pro CRM workflows in seven locales'
          git push origin HEAD:feat/p1-a-pro-crm-foundation
