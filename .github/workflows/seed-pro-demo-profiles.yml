name: Seed AQWELIA Pro demo profiles

on:
  push:
    branches:
      - main
    paths:
      - scripts/seed-pro-demo-profiles.mjs
      - .github/workflows/seed-pro-demo-profiles.yml

permissions:
  contents: read
  statuses: write

concurrency:
  group: aqwelia-pro-demo-seed
  cancel-in-progress: false

jobs:
  seed:
    name: Seed four AQWELIA Pro demo workspaces
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    env:
      DATABASE_URL: ${{ secrets.PRODUCTION_DIRECT_URL }}
    steps:
      - name: Publish pending status
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          curl --fail-with-body --silent --show-error \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -d "{\"state\":\"pending\",\"context\":\"AQWELIA Pro demo seed\",\"description\":\"Création des 4 espaces Pro de démonstration\",\"target_url\":\"${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}\"}"
      - uses: actions/checkout@v5
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.4'
      - run: bun install --frozen-lockfile
      - name: Validate direct production connection shape
        run: |
          node - <<'NODE'
          const url = process.env.DATABASE_URL || ''
          if (!/^postgres(ql)?:\/\//.test(url)) throw new Error('PRODUCTION_DIRECT_URL is required')
          if (new URL(url).hostname.includes('-pooler')) throw new Error('Demo seed must use the direct Neon connection')
          NODE
      - name: Generate PostgreSQL client
        run: bun run db:pg:generate
      - name: Seed AQWELIA Pro demo profiles
        id: seed_profiles
        run: node scripts/seed-pro-demo-profiles.mjs
      - name: Publish final status
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          SEED_OUTCOME: ${{ steps.seed_profiles.outcome }}
        run: |
          if [ "${SEED_OUTCOME}" = "success" ]; then
            STATE=success
            DESCRIPTION="4 espaces AQWELIA Pro créés et vérifiés"
          else
            STATE=failure
            DESCRIPTION="Échec de création des espaces AQWELIA Pro"
          fi
          curl --fail-with-body --silent --show-error \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -d "{\"state\":\"${STATE}\",\"context\":\"AQWELIA Pro demo seed\",\"description\":\"${DESCRIPTION}\",\"target_url\":\"${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}\"}"
