import{describe,it,expect}from'vitest';import{assessRecommendationOutcome,clampRating}from'@/lib/brain/outcome';describe('AQWELIA Brain',()=>{it('measures meaningful outcomes without overclaiming noise',()=>{expect(assessRecommendationOutcome(58,70)).toEqual({status:'improved',delta:12});expect(assessRecommendationOutcome(70,73).status).toBe('stable');expect(assessRecommendationOutcome(70,61).status).toBe('worsened')});it('keeps missing data inconclusive',()=>expect(assessRecommendationOutcome(undefined,80).status).toBe('inconclusive'));it('validates ratings',()=>{expect(clampRating(5)).toBe(5);expect(clampRating(0)).toBeNull()})})
