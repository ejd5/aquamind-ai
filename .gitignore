# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
package-lock.json
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
/.mobile-build-stash/

# production
/build
/generated/client-postgresql/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
# P0-FIX Bug 2: `local-*` rule above was meant for `next.config.local.ts`
# style overrides, but it also excluded the Capacitor wrapper
# `src/lib/native/local-notifications.ts` (the file the audit flagged as
# missing). Un-ignore that one specifically so it gets committed.
!src/lib/native/local-notifications.ts
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/
# AquaMind — local artifacts (ne pas pousser)
.env
!.env.example
.env.local
db/*.db
db/*.db-journal
*.png
!public/**/*.png
!src/app/icon.png
!src/app/apple-icon.png
!docs/**/*.png
upload/
tool-results/
keepalive*.sh
keepalive*.log
dev*.log
server.log
.zscripts/
examples/
mini-services/
# ARQWELIA Lot 2 — benchmark dataset (photos) and generated reports
dataset/photos/
benchmark-out/
# ARQWELIA Lot 2 Phase 0A — local NON-versioned retention manifest (JSON).
# Lives inside benchmark-out/, but explicit patterns keep it out of git even if
# the output dir is relocated. Never commit real photos or manifests.
**/benchmark-out/phase0a-manifest.json
benchmark-out/phase0a-manifest.json
# Phase 0A local manifest lock (transient, per read-modify-write).
**/benchmark-out/phase0a-manifest.lock
benchmark-out/phase0a-manifest.lock
